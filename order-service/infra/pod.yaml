apiVersion: v1
kind: Pod
metadata:
  name: order-service-bare
  labels:
    app: order-service
spec:
  containers:
    - name: order-service
      image: thecodealchemist/order-svc:1.0-SNAPSHOT
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 8080